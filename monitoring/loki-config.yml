auth_enabled: false

server:
  http_listen_port: 3100
  grpc_listen_port: 9096
  # ADDED: Increase timeouts and message sizes so heavy dashboard queries don't drop
  http_server_read_timeout: 300s
  http_server_write_timeout: 300s
  grpc_server_max_recv_msg_size: 104857600
  grpc_server_max_send_msg_size: 104857600

common:
  instance_addr: 127.0.0.1
  path_prefix: /loki
  storage:
    filesystem:
      chunks_directory: /loki/chunks
      rules_directory: /loki/rules
  replication_factor: 1
  ring:
    kvstore:
      store: inmemory

query_range:
  # ADDED: This forces Loki to break 30-day queries into 24h chunks and run them in parallel
  split_queries_by_interval: 24h
  results_cache:
    cache:
      embedded_cache:
        enabled: true
        max_size_mb: 100

schema_config:
  configs:
    - from: 2020-10-24
      store: tsdb
      object_store: filesystem
      schema: v13
      index:
        prefix: index_
        period: 24h

compactor:
  working_directory: /loki/boltdb-shipper-compactor
  retention_enabled: true
  delete_request_store: filesystem

limits_config:
  retention_period: 720h # 30 Tage
  # ADDED: These prevent Loki from rejecting large dashboard requests
  max_query_length: 720h 
  max_query_series: 100000
  max_query_parallelism: 2
  query_timeout: 300s 
  max_entries_limit_per_query: 10000